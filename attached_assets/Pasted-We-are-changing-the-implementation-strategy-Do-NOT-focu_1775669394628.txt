We are changing the implementation strategy.

Do NOT focus on fully automatic demo launch anymore.
We will focus on a reliable command-based workflow first.

New product direction:
1. FACEIT login via OAuth
2. FACEIT match list inside the app
3. manual/assisted demo processing from the Downloads folder
4. strong command support for CS2 demo playback
5. optional voice command setup before copying the command

Please implement the following exactly.

--------------------------------------------------
1. FACEIT LOGIN VIA OAUTH
--------------------------------------------------

Implement proper FACEIT OAuth login flow inside the app.

Requirements:
- Add a FACEIT login button
- Support OAuth redirect/callback flow
- Store the auth/token state cleanly
- Keep auth code separated in a dedicated service

Create or update:
- faceitAuthService.ts
- FaceitCallbackPage.tsx
- any needed auth state handling

IMPORTANT:
Also explain clearly what I need from FACEIT Developer Portal to make this work.

At the end, give me a checklist:
- what I need to create in FACEIT Developer
- which redirect URL I need
- which client id / keys are needed
- what environment variables I must set

Do not fake OAuth completion if credentials are missing.
Build the real structure and clearly mark what still needs real FACEIT developer setup.

--------------------------------------------------
2. DOWNLOADS FOLDER SCAN
--------------------------------------------------

We will not rely only on drag & drop anymore.

Implement a "Downloads folder scan" workflow:

- Add a button somewhere visible:
  "Downloads-Ordner scannen"

Behavior:
- scan the selected downloads folder
- detect new demo files:
  - .dem
  - .dem.gz
  - .dem.zst
- if new demo files are found:
  - process them
  - extract if needed
  - move/copy them into the configured CS2 replay/demo folder
  - register them in the local library

Settings:
- add a configurable downloads folder in settings
- default should be the normal Windows Downloads folder
- user can change it manually
- keep current replay folder logic too

If no downloads folder is set:
- auto-detect Windows default Downloads folder

Please make this reliable and explicit.
Do not silently scan random folders.

--------------------------------------------------
3. FACEIT MATCH DATA IMPROVEMENTS
--------------------------------------------------

Improve the FACEIT match view.

For each match, show more useful information such as:
- map name
- date/time
- match id
- demo availability
- download status
- local processed / not processed

If possible from available API response, also show:
- teams / opponent
- result / score

Keep UI text in German.

--------------------------------------------------
4. COMMAND-BASED PLAYBACK FLOW
--------------------------------------------------

We are now focusing on reliable command support, not pretending full automation works.

When the user clicks:
- "In CS2 öffnen"
or
- "Befehl kopieren"

The app should:
- generate the correct playdemo command
- copy it to clipboard
- optionally also start CS2 / Steam
- but the command workflow is now the reliable primary path

Important:
- In FACEIT match view, add a button to copy the command too
- If the demo has not yet been processed into the replay folder, block the action and show a clear message

Do not fake success by saying the demo started automatically if it did not.

--------------------------------------------------
5. VOICE COMMAND SUPPORT BEFORE COPYING COMMAND
--------------------------------------------------

Before copying the playback command, allow the user to choose a voice mode.

Add UI like this:
- Kein Voice
- Eigenes Team hören
- Gegner hören
- Alle hören

This selection should happen before the final command is copied.

Implement a dedicated voice service:

- voiceService.ts

Requirements:
- generate the needed CS2 voice-related command block
- append/prepend the correct commands before playdemo
- structure the code so that team-based logic can later become smarter

IMPORTANT:
If real team/player parsing is not yet possible from the demo:
- implement a clean placeholder architecture
- mark clearly where real demo parsing will be needed later
- do not pretend the logic is complete if it is not

For now, it is acceptable to:
- support "Alle hören" cleanly if possible
- structure the code for "Eigenes Team" / "Gegner" for future parsing
- clearly label incomplete modes if they are not yet technically possible

--------------------------------------------------
6. SETTINGS
--------------------------------------------------

Update settings page with:
- Downloads-Ordner
- Replay-/Demo-Ordner
- FACEIT auth status
- button to scan downloads folder
- button to open downloads folder
- button to open replay folder

--------------------------------------------------
7. UX REQUIREMENTS
--------------------------------------------------

Important UX rules:
- Do not overfocus on full automatic launch anymore
- Focus on reliability
- If a command is copied, show the exact copied command in a modal or panel
- Make it easy for the user to understand what to do next

Examples of good German messages:
- "Befehl wurde in die Zwischenablage kopiert."
- "Demo wurde im Replay-Ordner gespeichert."
- "Im Downloads-Ordner wurde keine neue Demo gefunden."
- "Für diese Voice-Option wird später echtes Demo-Parsing benötigt."

--------------------------------------------------
8. TECHNICAL STRUCTURE
--------------------------------------------------

Keep code cleanly separated:
- FACEIT auth
- FACEIT matches
- downloads scanning
- demo processing
- command generation
- voice command generation

Do not mix all logic into components.

--------------------------------------------------
9. DOCUMENTATION
--------------------------------------------------

Create or update documentation files:
- faceit-oauth-setup.md
- downloads-scan-flow.md
- voice-command-flow.md

Each file should explain:
- what is already implemented
- what still needs real credentials or real demo parsing
- how the feature is intended to work

--------------------------------------------------
10. IMPORTANT
--------------------------------------------------

Do not give me only a summary.
Actually patch the repo.

After the changes, tell me:
- which files were changed
- what still requires real FACEIT developer setup
- what parts are fully working
- what parts are only structured/prepared