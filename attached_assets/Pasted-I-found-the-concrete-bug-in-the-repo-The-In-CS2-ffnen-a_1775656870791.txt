I found the concrete bug in the repo.

The "In CS2 öffnen / ansehen" button is failing because the frontend and Rust backend do not agree on the success status.

Current bug:
- In src/services/cs2Service.ts, launchDemoInCS2() expects:
  result.status === "launched"
- But in src-tauri/src/lib.rs, launch_cs2() returns:
  status: "gestartet"

This means the frontend never recognizes a successful native launch and always behaves like launch failed.

Please fix this first before doing anything else.

Required fixes:

1. Fix the success status mismatch
- Make frontend and Rust use the exact same success status
- Prefer using "launched" everywhere for consistency
- Update any related typings if needed

2. Verify the actual native Tauri launch flow
- Right now the native Tauri path directly spawns cs2.exe with +playdemo
- It does NOT currently use Steam as primary launch
- Please confirm this in the code and explain it clearly

3. Improve the native launch implementation
I want this priority order:
A. Primary: Steam-based launch from Tauri on Windows
   Use a real Windows Steam handoff from Rust, not only browser window.open
B. Fallback: direct cs2.exe launch with the correct +playdemo argument
C. Last fallback: copy playdemo command to clipboard

4. Fix the Steam URI handling
- Inspect the current buildSteamLaunchUri() implementation
- If the current format is wrong or fragile, correct it
- Then make the native Tauri path actually use the Steam launch path as primary

5. Add launch debug logs
Please log:
- demo filename
- generated playdemo arg
- generated Steam URI
- whether Steam launch was attempted
- whether direct cs2.exe fallback was attempted
- exact status returned from Rust

6. Do NOT touch replay-folder logic right now
The demo file location and manual console command already work.
The remaining bug is the automatic launch/handoff only.

7. After patching, tell me:
- which files changed
- what the root cause was
- what the final launch flow is now