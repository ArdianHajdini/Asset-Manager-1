I checked the current GitHub code and found the concrete bug.

The "In CS2 öffnen / ansehen" button is still not correct.

Root cause #1:
The frontend expects a successful native launch result to return:
status === "launched"

But the Rust backend currently returns:
status: "gestartet"

So even if CS2 starts successfully, the frontend still treats it like failure and goes into fallback mode.

Root cause #2:
In native Tauri mode, the app still directly launches cs2.exe with:
+playdemo replays/<demo_name>

It does NOT currently use Steam as the primary native handoff.
The Steam URI only exists in the browser fallback path.

Please fix this concretely:

1. Fix the success status mismatch
- Make frontend and Rust use the exact same success status
- Use "launched" everywhere
- Do not keep mixed statuses like "gestartet"

2. Fix the native launch flow
I want this priority order:
A. Primary: Steam-based native launch from Rust on Windows
B. Fallback: direct cs2.exe launch with +playdemo replays/<name>
C. Last fallback: copy the playdemo command to clipboard

3. Fix the Steam launch implementation
- Do not leave Steam launch only in the browser path
- Implement real Steam handoff in the Tauri Rust backend
- Log the exact Steam command / URI used

4. Inspect the current buildSteamLaunchUri() function
- It currently builds:
  steam://rungame/730/0/+playdemo+"<arg>"
- Verify whether this format is actually correct
- If not, replace it with the correct format and use the corrected version in the native path

5. Add launch debug logs
Please log:
- demo filename
- generated playdemo arg
- exact Rust-side launch method attempted
- exact Steam URI or command used
- whether direct cs2.exe fallback was attempted
- final LaunchResult.status returned to frontend

6. Do NOT change replay folder logic now
The manual console command already works, so the remaining bug is automatic launch only.

7. After patching, tell me exactly:
- which files changed
- what the root cause was
- what exact status string is now returned
- what the final native launch order is