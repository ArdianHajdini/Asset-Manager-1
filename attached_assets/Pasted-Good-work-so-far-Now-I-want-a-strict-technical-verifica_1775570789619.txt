Good work so far.

Now I want a strict technical verification, not a summary.

Please answer ALL of the following questions clearly and concretely:

----------------------------
1. Tauri Build Status
----------------------------

- Has this project actually been built and run as a real Tauri desktop app?
- Or is it only prepared structurally?

If yes:
- show the exact command used
- show the output/result

----------------------------
2. Real Filesystem (IMPORTANT)
----------------------------

When I import a demo:

- Is the file actually written to disk?
- Or is it only stored in memory / localStorage?

Show:
- the exact Rust function handling file write
- the exact path used

----------------------------
3. .dem.gz Extraction
----------------------------

- Is .dem.gz extraction fully implemented?

If yes:
- which Rust crate is used?
- show the exact extraction code
- confirm the output file is a real .dem file on disk

If not:
- clearly say it's not implemented

----------------------------
4. Demo Library
----------------------------

- Does the app read demo files from the real filesystem?
- Or is the library still simulated?

Show:
- Rust function that lists demo files
- how the frontend receives them

----------------------------
5. FACEIT Integration
----------------------------

- Is FACEIT login actually implemented?
- Or only UI prepared?

- Can the app currently fetch real matches from FACEIT?
- Can it download demos directly from FACEIT?

Be very explicit:
- working
- partially implemented
- not implemented

----------------------------
6. CS2 Launch (CRITICAL)
----------------------------

- Does the app actually launch CS2 on the system?

Explain exactly:

- which method is used:
  - Steam URI?
  - direct cs2.exe launch?
  - shell command?

- show the Rust implementation

- confirm:
  does it actually open CS2 when triggered?

----------------------------
7. Voice Feature
----------------------------

- Is the voice feature fully implemented?

Specifically:
- are tv_listen_voice_indices calculated correctly?
- is bitmask logic real or placeholder?
- where is the calculation code?

----------------------------
8. Windows Compatibility
----------------------------

- Has any part of this been tested on Windows?
- Or only inside the Linux/Replit environment?

Be honest.

----------------------------
9. What is STILL missing
----------------------------

Give me a clear checklist:

What must be done before this becomes a fully working Windows .exe that:
- imports demos
- extracts .gz
- launches CS2
- supports voice mode
- optionally supports FACEIT

----------------------------
IMPORTANT
----------------------------

Do not give a general explanation.

Give concrete answers with code references.
If something is not implemented, say it directly.